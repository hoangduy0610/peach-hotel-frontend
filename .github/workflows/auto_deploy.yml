name: Auto Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Change directory
        run: cd ~/hotel-management-fe

      - name: Pull latest code
        run: git pull
      
      - name: Install dependencies
        run: yarn

      - name: Start
        run: yarn start
